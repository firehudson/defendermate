{
  "name": "defendermate",
  "private": true,
  "scripts": {
    "backend": "pnpm --filter backend dev",
    "frontend": "pnpm --filter frontend dev"
  },
  "pnpm": {
    "onlyBuiltDependencies": ["@nestjs/core", "@prisma/engines", "bcrypt", "prisma", "sharp"]
  }
}
